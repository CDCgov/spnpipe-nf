params {
    read_dir = ""
    results_dir = ""
    script_dir = "$projectDir"
    db_dir = "$projectDir/SPN_Reference_DB"
}

profiles {
    standard {
        process {
            memory = "8GB"
            cpus = 4
        }
        docker {
            enabled = true
	    }
    }    
}